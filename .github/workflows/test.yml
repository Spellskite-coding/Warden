name: Test
on: [push, pull_request]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - run: cargo test -p warden-ransomware -p warden-common
      - run: cargo clippy -p warden-ransomware -- -D warnings